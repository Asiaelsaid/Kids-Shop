import { FormetCardPipe } from './formet-card.pipe';

describe('FormetCardPipe', () => {
  it('create an instance', () => {
    const pipe = new FormetCardPipe();
    expect(pipe).toBeTruthy();
  });
});
